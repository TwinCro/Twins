package com.dungeonrealms;

public class Main {
    public static void main(String[] args) {
        System.err.println("DungeonRealms is a Minecraft plugin, not a standalone application.");
        System.err.println("To use it: build with 'mvn package', then place the resulting");
        System.err.println("DungeonRealms.jar into your Paper server's plugins/ folder");
        System.err.println("and start the server.");
        System.exit(1);
    }
}
